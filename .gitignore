# Claude Code internals — không track
sessions/
backups/
file-history/
shell-snapshots/
session-env/
telemetry/
ide/
plans/
.last-cleanup

# Sensitive
.credentials.json
mcp-needs-auth-cache.json
settings.local.json

# Projects — chỉ track memory/, bỏ session data
projects/**/*.jsonl
projects/**/subagents/
projects/**/tool-results/

# Track ở repo này:
# CLAUDE.md           ← global profile + universal principles
# settings.json       ← non-sensitive config
# .gitignore          ← file này
# projects/*/memory/  ← cognition system (tất cả projects)
